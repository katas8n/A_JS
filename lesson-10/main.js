import "./style.css";

// const registeredList = []

// User schema => { login : ,password : , hasRemembered: }

// If user has already registered and was logedIn -> he should look at "Log out" so "Register" button must be hidden
// If user hasn't registered yet -> he should look at "Register"

// If user clicked on "Login" btn => He should get two fields -> login / password

// Only users which have already have an account can see "Profile"
